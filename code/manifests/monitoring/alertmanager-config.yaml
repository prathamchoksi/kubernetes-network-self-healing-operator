apiVersion: monitoring.coreos.com/v1alpha1
kind: AlertmanagerConfig
metadata:
  name: webhook-config
  namespace: monitoring
  labels:
    release: prometheus
spec:
  route:
    receiver: operator-webhook
    groupBy: ["alertname"]
    groupWait: 10s
    groupInterval: 1m
    repeatInterval: 5m
  receivers:
    - name: operator-webhook
      webhookConfigs:
        - url: http://network-operator-webhook.kube-system.svc:8080/webhook
          sendResolved: false
