kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: k8s-network-healing
networking:
  disableDefaultCNI: true  # We'll install Calico instead
nodes:
  - role: control-plane
  - role: worker
  - role: worker
